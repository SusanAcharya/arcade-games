import React from 'react';
import DiceShootout from './DiceShootout';

const DiceShootoutB2: React.FC = () => {
  return <DiceShootout mode='B2' skipSelect={true} />;
};

export default DiceShootoutB2;
